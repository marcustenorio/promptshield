@startuml
!include ./vendor/C4_Context.puml
LAYOUT_WITH_LEGEND()

Person(user, "Usuário", "Envia perguntas via HTTP para o sistema.")
System_Boundary(ps, "PromptShield API") {
  System(ps_api, "FastAPI App", "Firewall semântico + orquestração do LLM.")
}
System_Ext(gemini, "Gemini API (Google GenAI)", "LLM para geração de respostas.")
System_Ext(storage, "FS/Artefatos", "Datasets, modelos e logs.")

Rel(user, ps_api, "HTTP /chat", "JSON/REST")
Rel(ps_api, gemini, "generateContent()", "Google GenAI SDK (v1)")
Rel(ps_api, storage, "lê/escreve", "FS")

SHOW_LEGEND()
@enduml

